{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://lancer-job-board.local/schemas/theater-location.schema.json",
  "title": "Theater Location",
  "description": "A location marker placed on an operational theater",
  "type": "object",
  "required": [
    "id",
    "name",
    "assignedJobIds"
  ],
  "properties": {
    "id": {
      "type": "string",
      "description": "Unique identifier (UUID)",
      "minLength": 1
    },
    "name": {
      "type": "string",
      "description": "Location name",
      "minLength": 1
    },
    "description": {
      "type": "string",
      "description": "Location description (optional)"
    },
    "icon": {
      "type": "string",
      "description": "SVG filename from logo_art directory used as the marker icon",
      "pattern": "^[A-Za-z0-9_-]+\\.svg$"
    },
    "iconColor": {
      "type": "string",
      "description": "CSS color used to tint/highlight the marker"
    },
    "iconScale": {
      "type": "number",
      "description": "Scale multiplier for the marker icon",
      "minimum": 0.1
    },
    "x": {
      "type": ["number", "null"],
      "description": "Normalized horizontal position (0..1) for flat theaters",
      "minimum": 0,
      "maximum": 1
    },
    "y": {
      "type": ["number", "null"],
      "description": "Normalized vertical position (0..1) for flat theaters",
      "minimum": 0,
      "maximum": 1
    },
    "lat": {
      "type": ["number", "null"],
      "description": "Latitude in degrees for planet theaters (Iteration 2)",
      "minimum": -90,
      "maximum": 90
    },
    "lon": {
      "type": ["number", "null"],
      "description": "Longitude in degrees for planet theaters (Iteration 2)",
      "minimum": -180,
      "maximum": 180
    },
    "assignedJobIds": {
      "type": "array",
      "description": "Job IDs assigned to this location (references into jobs.json)",
      "items": { "type": "string" }
    },
    "childTheaterId": {
      "type": ["string", "null"],
      "description": "Optional theater ID to drill into from this location (sublocations)"
    }
  },
  "additionalProperties": false
}