version: '3.8'

services:
  app:
    build: .
    ports:
      - "${PORT:-3000}:${PORT:-3000}"
    volumes:
      - ./data:/app/data
    environment:
      - PORT=${PORT:-3000}
      - CLIENT_KEY=${CLIENT_KEY:-IMHOTEP}
      - ADMIN_KEY=${ADMIN_KEY:-TARASQUE}
      - NODE_ENV=production
